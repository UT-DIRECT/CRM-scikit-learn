from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This repository is going to evaluate how well the CRM model predicts reservoir production.',
    author='akhilpotla',
    license='',
)
